<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Spring Batch Job Dashboard</title>
    <link rel="stylesheet" th:href="@{/css/bootstrap.min.css}">
</head>
<body>
<div class="container mt-4">
    <h2 class="mb-4">🔄 Batch Job Dashboard</h2>

    <!-- Job Instance Table -->
    <table class="table table-bordered">
        <thead class="table-dark">
        <tr>
            <th>Instance ID</th>
            <th>Job Name</th>
            <th>Executions</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="instance : ${instanceList}">
            <td th:text="${instance['instanceId']}"></td>
            <td th:text="${instance['jobName']}"></td>
            <td>
                <table class="table table-sm table-striped">
                    <thead>
                    <tr>
                        <th>Execution ID</th>
                        <th>Status</th>
                        <th>Exit Code</th>
                        <th>Start Time</th>
                        <th>End Time</th>
                        <th>Action</th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr th:each="exec : ${instance['executions']}">
                        <td th:text="${exec['executionId']}"></td>
                        <td th:text="${exec['status']}"
                            th:classappend="${exec['status']} == 'FAILED' ? 'text-danger' :
                                            (exec['status'] == 'COMPLETED' ? 'text-success' :
                                            (exec['status'] == 'STARTED' ? 'text-primary' :
                                            (exec['status'] == 'STARTING' ? 'text-warning' : '')))">
                        </td>
                        <td th:text="${exec['exitCode']}"></td>
                        <td th:text="${exec['startTime']}"></td>
                        <td th:text="${exec['endTime']} != null ? ${exec['endTime']} : '-' "></td>
                        <td>
                            <form th:action="@{'/stop/' + ${exec['executionId']}}" method="post">
                                <button type="submit" class="btn btn-sm btn-outline-danger"
                                        th:disabled="${exec['status']} != 'STARTED' and ${exec['status']} != 'STARTING'">
                                    Stop
                                </button>
                            </form>
                        </td>
                    </tr>
                    </tbody>
                </table>
            </td>
        </tr>
        </tbody>
    </table>

    <!-- New Job Form -->
    <h4 class="mt-5">🚀 Launch New Batch Job</h4>
    <form th:action="@{/update-key}" method="post" class="mt-3">
        <div class="mb-3">
            <label class="form-label">Table Name</label>
            <input type="text" name="tableName" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Source Column</label>
            <input type="text" name="sourceColumn" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Destination Column</label>
            <input type="text" name="destinationColumn" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Old Key</label>
            <input type="password" name="oldKey" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">New Key</label>
            <input type="password" name="newKey" class="form-control" required>
        </div>
        <button type="submit" class="btn btn-success">Start Job</button>
    </form>
</div>
</body>
</html>
