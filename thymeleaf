<!DOCTYPE html>
<html xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <title>Spring Batch Job Dashboard</title>
    <link rel="stylesheet" th:href="@{/css/bootstrap.min.css}" />
</head>
<body>
<div class="container mt-4">
    <h2 class="mb-4">🔄 Batch Job Dashboard</h2>

    <table class="table table-bordered table-striped">
        <thead class="table-dark">
        <tr>
            <th>Job Name</th>
            <th>Executions</th>
            <th>Status</th>
            <th>Exit Code</th>
            <th>Start Time</th>
            <th>End Time</th>
            <th>Action</th>
        </tr>
        </thead>
        <tbody>
        <tr th:each="instance : ${instanceList}">
            <td th:text="${instance.jobName}" rowspan="${#lists.size(instance.executions)}"></td>
            <td colspan="6" th:if="${#lists.isEmpty(instance.executions)}">No Executions</td>

            <tr th:each="exec : ${instance.executions}">
                <td th:text="${exec.executionId}"></td>
                <td>
                    <span th:switch="${exec.status}">
                        <span th:case="'COMPLETED'" class="badge bg-success" th:text="${exec.status}"></span>
                        <span th:case="'FAILED'" class="badge bg-danger" th:text="${exec.status}"></span>
                        <span th:case="'STARTED'" class="badge bg-warning text-dark" th:text="${exec.status}"></span>
                        <span th:case="*" class="badge bg-secondary" th:text="${exec.status}"></span>
                    </span>
                </td>
                <td th:text="${exec.exitCode}"></td>
                <td th:text="${exec.startTime}"></td>
                <td th:text="${exec.endTime}"></td>
                <td>
                    <form th:action="@{'/job/stop/' + ${exec.executionId}}" method="post">
                        <button type="submit" class="btn btn-sm btn-outline-danger"
                                th:disabled="${exec.status} != 'STARTED'">Stop</button>
                    </form>
                </td>
            </tr>
        </tr>
        </tbody>
    </table>

    <!-- Launch Form -->
    <h4 class="mt-5">🚀 Launch New Batch Job</h4>
    <form action="/job/update-key" method="post" class="mt-3">
        <div class="mb-3">
            <label class="form-label">Table Name</label>
            <input type="text" name="tableName" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Source Column</label>
            <input type="text" name="sourceColumn" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Destination Column</label>
            <input type="text" name="destinationColumn" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">Old Key</label>
            <input type="password" name="oldKey" class="form-control" required>
        </div>
        <div class="mb-3">
            <label class="form-label">New Key</label>
            <input type="password" name="newKey" class="form-control" required>
        </div>
        <button type="submit" class="btn btn-success">Start Job</button>
    </form>
</div>
</body>
</html>
